apiVersion: v1
kind: Service
metadata:
  name: heap-test-different
  namespace: heap-test
spec:
  type: NodePort
  selector:
    app: heap-test
    variant: xms-less-xmx
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082
