# 자체 작성 소스 파일 크기 제한

## 결정

- 자체 작성 코드 파일을 모두 1,000줄 미만으로 유지
- vendored 소스, 생성 파일, lock 파일, 아이콘 제외
- AGENTS.md와 CI의 `npm run check:size`로 강제

## 이유

- 모듈 책임과 분리 시점을 파일 구조에서 식별
- 화면 교체와 기능 추가 시 한 파일에 책임이 누적되는 문제 방지
