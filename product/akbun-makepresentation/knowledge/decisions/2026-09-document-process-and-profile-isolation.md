---
type: Decision
title: 문서는 별도 프로세스와 파일별 프로필로 분리
description: 편집 상태와 AI 런타임뿐 아니라 설정·대화 저장 공간도 문서별로 격리.
tags: [tauri, desktop, isolation, storage]
timestamp: 2026-09-20T00:00:00Z
---

## 결정

- 문서를 보고 있는 창의 파일 열기와 새 문서는 현재 실행 파일을 별도 프로세스로 시작.
- 파일이 없는 창은 여는 파일을 제자리에서 맡음. Rust가 프로필을 그 파일 것으로 바꾸고 페이지가 설정과 AI 서버를 다시 시작.
- macOS Opened 이벤트는 페이지가 시작 문서를 묻기 전이면 시작 문서로, 뒤면 페이지 이벤트로 전달.
- 덱·선택·실행 취소·검색·확대 상태와 Codex 런타임은 프로세스가 소유.
- 정규화한 파일 경로의 해시를 프로필 UUID에 연결.
- 설정·프리셋·AI 대화·AI 임시 이미지는 프로필 아래 저장.
- 프로필 잠금이 사용 중이면 저장된 설정·대화를 복제하고 별도 잠금 획득.
- 새 문서는 최초 저장에서 경로 연결, Save As는 원래 경로의 프로필 사본 보존.
- 같은 경로를 여러 앱에서 저장하면 마지막으로 저장한 프로필을 다음 열기에 사용.

## 이유

- 창만 늘리면 전역 AI 런타임과 이벤트가 다른 문서에 영향을 줄 수 있음.
- PID를 저장 경로로 쓰면 앱을 다시 열 때 설정·대화를 찾을 수 없음.
- 프로필 잠금은 프로세스가 종료되면 운영체제가 해제해 비정상 종료에도 재사용 가능.
- 시스템 클립보드는 앱 간 복사에 필요한 명시적 공유 경계.
- 빈 창이 새 프로세스를 띄우고 스스로 닫는 방식은 창이 잠시 또는 계속 2개로 남았음. Finder 더블클릭은 argv가 아니라 Opened 이벤트라 빈 프로세스가 먼저 떠 있었음.

관련: [AI 세션 소유권](2026-08-ai-sessions-are-app-owned.md), [시스템 복사 후 잘라내기](2026-09-cut-runs-the-copy-handler.md)
