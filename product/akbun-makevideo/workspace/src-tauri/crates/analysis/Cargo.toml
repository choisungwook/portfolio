[package]
name = "makevideo-analysis"
version = "0.1.0"
description = "Provider-neutral transcription and silence analysis types"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
