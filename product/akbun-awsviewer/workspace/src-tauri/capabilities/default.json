{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to ask for. Anything missing here fails at runtime rather than at compile time, so this list is deliberately short: the page only drives the self updater, and every AWS or file system call goes through a command in commands.rs. The SSO login window is not listed, so the remote identity page it shows gets no IPC at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-message",
    "dialog:allow-ask",
    "log:default",
    "updater:allow-check",
    "updater:allow-download-and-install",
    "process:allow-restart"
  ]
}
