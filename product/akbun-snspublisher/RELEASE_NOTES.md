# Release notes

## 0.1.0

- Product skeleton: Worker entry point with Access and token verification, D1 token migration, verify workflow.
- Three screens on mock data: compose with per-channel character counters, queue ordered by schedule, month calendar.
- No deployment yet. The Worker answers `/api/health` and `/api/me` only.
