# Agent Guide

## 저장소의 목적

- 핸즈온 실습과 공부 노트 저장소다. 문서는 핸즈온 절차와 그에 필요한 이론만 담는다. 원리를 이해하고 재현할 수 있으면 충분하다.
- 이 저장소는 공개되므로 코드와 문서에 문맥상 민감한 정보를 쓰지 않는다. 실습은 기술 자체로 성립하도록 쓴다.

## 코드 규칙

코드 작성 규칙은 `.claude/rules/`에 있다. 작업 대상에 해당하는 파일을 따른다.

## 문서 작성 규칙

문서의 목적은 핸즈온과 그에 관련된 이론 정리다.

문체:

- 개조식으로 쓴다. 항목을 나열하고 각 항목은 짧게 끊는다. 서술형 문단으로 길게 풀지 않는다.
- 한 항목에는 한 가지만 담는다. 내용이 둘이면 항목을 나눈다.
- 원리 설명이나 판단 근거처럼 개조식으로 끊으면 흐름이 끊기는 부분만 산문으로 쓴다.

내용:

- 제3자가 읽었을 때 의미 없는 컨텍스트를 쓰지 않는다. 작업 당시의 대화 맥락, agent의 작업 과정, "요청에 따라 ~했다" 같은 문장을 금지한다.
- 결론을 먼저 쓰고, 필요한 경우에만 세부 설명을 붙인다.
- 표, 목록, 코드가 산문보다 간결하면 그것을 우선 사용한다.
- 추측이 필요하면 가정만 짧게 밝히고 진행한다.

구조:

- 새 예제 디렉터리의 `README.md`는 문서 링크 허브로만 쓴다. 긴 설명과 실습 절차는 `docs/` 디렉터리에 시나리오별 Markdown으로 분리한다.
- 실습 환경 구축(설치·기동·apply·접속·정리 등)은 내용 문서에 섞지 않고 별도 문서로 분리한다. 파일명은 `{번호}-setup.md` 컨벤션을 따르고, 내용 문서에서는 환경 준비를 그 setup 문서로 링크만 건다. 환경이 여러 개면 환경마다 setup 문서를 둔다. 다른 예제의 환경을 재사용하면 그 예제의 setup 문서로 링크한다.
- 새 핸즈온을 추가하면 루트 `README.md` 목차에 `{번호}. {주제} ({작성날짜}) - [링크](...)` 형식으로 추가한다. 날짜 예: `26.6.21`

## 지식 축적

작업에서 얻은 의사결정, 반복 절차, 도메인 통찰은 `knowledge/`에 기록해 다음 작업의 컨텍스트로 재사용한다. 기본 위치는 작업한 디렉터리 안의 `knowledge/`이고, 루트 [knowledge/](./knowledge/index.md)는 형식의 템플릿이자 저장소 전체에 영향을 주는 지식의 자리다. 세부 규칙은 [.claude/rules/knowledge.md](./.claude/rules/knowledge.md)를 따른다.

## 검증

변경은 돌려 보고 확인한다. 다만 사용자에게 확인을 떠넘기지 않는 것과 사용자의 화면을 뺏지 않는 것을 동시에 지켜야 하므로, 검증 수단에 순서를 둔다. 위 단계로 되는 검증을 아래 단계로 하지 않는다.

1. 데스크톱 앱(Claude Desktop, ChatGPT Desktop)의 내장 브라우저를 먼저 쓴다. 페이지를 띄우고 DOM, console, network를 읽고 클릭과 입력까지 여기서 끝낸다.
   - 내장 브라우저는 앱 안의 패널이라 화면과 마우스를 잡지 않는다. 사용자가 컴퓨터를 쓰는 중에도 검증이 돈다.
   - 사용자의 실제 브라우저(로그인 세션이 있는 Chrome)는 기본값이 아니다. 그 세션이 반드시 필요할 때만 쓴다.
   - `product/`의 no-build-step 앱은 `workspace/src`를 정적 서버로 띄우면 내장 브라우저에서 그대로 돈다. 화면 검증을 위해 Tauri나 Electron 번들을 빌드할 이유가 없다.
2. 브라우저로 확인할 수 없는 로직은 그 workspace의 테스트 명령(`npm test`, `cargo test` 등)으로 검증한다.
3. 1과 2로 확인할 수 없는 것만 computer use로 검증한다. 트레이 아이콘, 네이티브 메뉴, 창 동작처럼 화면 밖에 답이 없는 경우다.
   - 마지막 순위지 금지가 아니다. 여기까지 왔으면 허락을 묻느라 멈추지 말고 진행한다. 사용자가 자리에 없는 경우가 더 많고, 멈춰 세우는 쪽이 손해다.
   - 1이나 2로 되는 검증을 computer use로 하지 않는다. 편해서 고르는 것이지 필요해서 고르는 것이 아니면 순위를 어긴 것이다.
   - 로컬 앱 번들은 이 단계에 실제로 도달했을 때만 만든다.
   - computer use로 무엇을 확인했는지 결과 보고에 한 줄 남긴다. 사용자가 자기 화면에서 벌어진 일을 나중에 알게 되면 안 된다.

- 로컬 검증에서 배포, 설치 패키지, updater 산출물, 서명 명령을 실행하지 않는다.
- 배포 산출물 검증은 사용자가 이 대화에서 명시적으로 지시한 경우에만 수행한다.

## 작업 흐름

PR body 형식의 기준은 [.github/pull_request_template.md](./.github/pull_request_template.md) 하나다. PR을 쓰기 전에 이 파일을 읽고 그 섹션과 항목 형식을 그대로 따른다. 형식을 이 문서나 `.claude/rules/`에 중복해 적지 않는다.
