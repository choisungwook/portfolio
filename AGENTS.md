# Agent Guide

## 저장소의 목적

- 핸즈온 실습과 공부 노트, 그리고 직접 만든 제품을 함께 두는 저장소다. 대부분은 핸즈온이고, `product/` 아래만 릴리스와 자동 업데이트까지 가는 완성형 도구다.
- 핸즈온 문서는 실습 절차와 그에 필요한 이론만 담는다. 원리를 이해하고 재현할 수 있으면 충분하다.
- 이 저장소는 공개되므로 코드와 문서에 문맥상 민감한 정보를 쓰지 않는다. 실습은 기술 자체로 성립하도록 쓴다.

## 규칙 로딩

규칙은 두 곳에 나뉘어 있다. 컨텍스트를 예산으로 다루기 위해서다.

- `.claude/rules/`는 매 세션 자동으로 주입된다. 글쓰기 철학, Markdown, knowledge, 작업 흐름처럼 거의 모든 작업에 걸리는 규칙만 둔다.
- `.claude/rule-details/`는 자동으로 주입되지 않는다. Terraform, Kubernetes, Tauri, Electron, Python, product, GitHub 도구처럼 특정 작업에만 걸리는 규칙이다.

**MANDATORY**: 작업이 `.claude/rule-details/`의 어느 영역에 닿으면, 그 영역의 파일을 **파일을 편집하기 전에 읽는다**. 어느 작업에 어느 파일이 걸리는지는 [.claude/rules/index.md](./.claude/rules/index.md)의 표에 있다.

- 규칙을 읽지 않고 한 작업은 그 자체로 결함이다. 규칙에 적힌 것은 대부분 이미 밟은 지뢰다.
- 편집한 뒤에 읽는 것은 늦다. 그때는 이미 규칙과 어긋난 코드가 있다.
- 이전 세션에서 읽은 것은 이번 세션에서 읽은 것이 아니다. 기억으로 대신하지 않는다.

## 인터뷰와 진행

판단이 갈리는 지점에서 묻지 않고 고른 것은 결함이다. 되돌리는 비용이 묻는 비용보다 항상 크다.

- 저장소 탐색이나 웹 검색으로 확인되는 사실은 묻지 않는다. 그것은 조사할 것이지 물어볼 것이 아니다.
- 사용자만 답할 수 있는 것은 반드시 묻는다. 제품 이름, 기술 방향, 파일 형식, 기본값, 작업 범위, 우선순위가 여기에 든다.
- 선택지마다 트레이드오프를 한 줄로 붙이고 추천안을 표시한다.
- 사용자가 잘 모르는 기술 영역이면 질문 안에서 배경 개념부터 짧게 풀고 묻는다.
- 인터뷰에서 내린 결정은 그 작업의 Issue ADR에 기록한다.

묻는 것과 멈추는 것은 다르다. 사용자는 자리에 없는 경우가 더 많으므로, 물어야 할 것이 없으면 가정을 명시하고 진행한다. 답을 기다리느라 아무것도 만들지 않은 채 끝내지 않는다. 이 원칙은 검증과 작업 흐름을 포함한 모든 단계에 걸린다.

## 문서 작성 규칙

문서의 목적은 핸즈온과 그에 관련된 이론 정리다.

문체:

- 개조식으로 쓴다. 항목을 나열하고 각 항목은 짧게 끊는다. 서술형 문단으로 길게 풀지 않는다.
- 한 항목에는 한 가지만 담는다. 내용이 둘이면 항목을 나눈다.
- 원리 설명이나 판단 근거처럼 개조식으로 끊으면 흐름이 끊기는 부분만 산문으로 쓴다.

내용:

- 제3자가 읽었을 때 의미 없는 컨텍스트를 쓰지 않는다. 작업 당시의 대화 맥락, agent의 작업 과정, "요청에 따라 ~했다" 같은 문장을 금지한다.
- 결론을 먼저 쓰고, 필요한 경우에만 세부 설명을 붙인다.
- 표, 목록, 코드가 산문보다 간결하면 그것을 우선 사용한다.

구조:

- 새 예제 디렉터리의 `README.md`는 문서 링크 허브로만 쓴다. 긴 설명과 실습 절차는 `docs/` 디렉터리에 시나리오별 Markdown으로 분리한다.
- 실습 환경 구축(설치·기동·apply·접속·정리 등)은 내용 문서에 섞지 않고 별도 문서로 분리한다. 파일명은 `{번호}-setup.md` 컨벤션을 따르고, 내용 문서에서는 환경 준비를 그 setup 문서로 링크만 건다. 환경이 여러 개면 환경마다 setup 문서를 둔다. 다른 예제의 환경을 재사용하면 그 예제의 setup 문서로 링크한다.
- 새 핸즈온을 추가하면 루트 `README.md` 목차에 `{번호}. {주제} ({작성날짜}) - [링크](...)` 형식으로 추가한다. 날짜 예: `26.6.21`

## 지식

**MANDATORY**: 이미 있는 workspace를 고치기 전에 그 workspace의 `knowledge/index.md`를 읽는다. `wiki/`나 `adr/`가 있으면 그 index도 읽는다. 여기에는 코드와 git history가 기록하지 못한 결정의 이유가 있고, 읽지 않으면 이미 버려진 방법을 다시 고른다.

작업에서 얻은 의사결정, 반복 절차, 도메인 통찰은 작업한 디렉터리 안의 `knowledge/`에 기록해 다음 작업의 컨텍스트로 재사용한다. workspace마다 자기 `knowledge/`를 가지며 루트에는 두지 않는다. 형식 템플릿은 [templates/knowledge/](./templates/knowledge/index.md)이고, 세부 규칙은 [.claude/rules/knowledge.md](./.claude/rules/knowledge.md)를 따른다.

## 검증

변경은 돌려 보고 확인한다. 다만 사용자에게 확인을 떠넘기지 않는 것과 사용자의 화면을 뺏지 않는 것을 동시에 지켜야 하므로, 검증 수단에 순서를 둔다. 위 단계로 되는 검증을 아래 단계로 하지 않는다.

1. 데스크톱 앱(Claude Desktop, ChatGPT Desktop)의 내장 브라우저를 먼저 쓴다. 내장 브라우저가 없는 환경(웹 세션, CI)이면 이 단계를 건너뛰고 2로 내려간다. 페이지를 띄우고 DOM, console, network를 읽고 클릭과 입력까지 여기서 끝낸다.
   - 내장 브라우저는 앱 안의 패널이라 화면과 마우스를 잡지 않는다. 사용자가 컴퓨터를 쓰는 중에도 검증이 돈다.
   - 사용자의 실제 브라우저(로그인 세션이 있는 Chrome)는 기본값이 아니다. 그 세션이 반드시 필요할 때만 쓴다.
   - `product/`의 no-build-step 앱은 `workspace/src`를 정적 서버로 띄우면 내장 브라우저에서 그대로 돈다. 화면 검증을 위해 Tauri나 Electron 번들을 빌드할 이유가 없다.
2. 브라우저로 확인할 수 없는 로직은 그 workspace의 테스트 명령(`npm test`, `cargo test` 등)으로 검증한다.
3. 1과 2로 확인할 수 없는 것만 computer use로 검증한다. 트레이 아이콘, 네이티브 메뉴, 창 동작처럼 화면 밖에 답이 없는 경우다.
   - 마지막 순위지 금지가 아니다. 여기까지 왔으면 허락을 묻느라 멈추지 말고 진행한다. "인터뷰와 진행"이 그대로 적용된다.
   - 1이나 2로 되는 검증을 computer use로 하지 않는다. 편해서 고르는 것이지 필요해서 고르는 것이 아니면 순위를 어긴 것이다.
   - 로컬 앱 번들은 이 단계에 실제로 도달했을 때만 만든다.
   - computer use로 무엇을 확인했는지 결과 보고에 한 줄 남긴다. 사용자가 자기 화면에서 벌어진 일을 나중에 알게 되면 안 된다.

- 로컬 검증에서 배포, 설치 패키지, updater 산출물, 서명 명령을 실행하지 않는다.
- 배포 산출물 검증은 사용자가 이 대화에서 명시적으로 지시한 경우에만 수행한다.

## 작업 흐름

단계가 3개를 넘는 작업은 실행 계획을 `.claude/work/<branch>.md`에 남기고, 단계를 끝낸 그 인터랙션에서 체크박스를 갱신한다. 세션이 끊긴 뒤 재개하는 유일한 진입점이다. 형식과 삭제 시점은 [.claude/rules/workflow.md](./.claude/rules/workflow.md)의 "작업 상태 파일"에 있다.

PR body 형식의 기준은 [.github/pull_request_template.md](./.github/pull_request_template.md) 하나다. PR을 쓰기 전에 이 파일을 읽고 그 섹션과 항목 형식을 그대로 따른다. 형식을 이 문서나 `.claude/rules/`에 중복해 적지 않는다.

여러 줄인 Issue와 PR body는 UTF-8 Markdown 파일로 만든 뒤 `--body-file`로 전달한다. `--body`에 이스케이프한 줄바꿈을 넣지 않는다. 생성 직후 GitHub에서 body를 다시 읽어 실제 줄바꿈과 Issue 링크를 확인하고, 깨졌으면 리뷰 전에 고친다.

작업이 끝난 branch를 master에 넣는 전체 흐름은 [.claude/commands/repo-pr-ship.md](./.claude/commands/repo-pr-ship.md) 하나로 돈다. Issue와 PR 생성, Copilot 리뷰 요청, 리뷰 반영, squash merge, Issue close 순서다. 단계 하나만 필요하면 그 단계의 command를 직접 쓴다.

squash merge commit 제목 끝에는 `(#<PR 번호>)`를 붙인다. commit에서 원본 PR을 바로 열 수 있게 하는 추적 정보다.

GitHub 조작은 환경에 따라 도구가 갈린다. shell과 gh CLI가 있는 환경과 GitHub MCP 도구만 있는 환경이고, 절차는 같고 도구만 다르다. 판별 방법과 도구 대응표는 [.claude/rule-details/github-tools.md](./.claude/rule-details/github-tools.md)에 있다. MCP 환경에서는 commit, push, 대기가 불가능하므로 그 단계를 흉내 내지 않고 안내로 대체한다.
