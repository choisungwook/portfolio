kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kgateway
nodes:
  - role: control-plane
    extraPortMappings:
      # gateway NodePort 30080을 맥 localhost:8080으로 노출한다
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
