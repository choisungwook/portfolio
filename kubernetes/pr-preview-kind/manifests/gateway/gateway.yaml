apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: pr-preview-gateway
  namespace: pr-preview
  labels:
    app.kubernetes.io/name: pr-preview-gateway
    app.kubernetes.io/part-of: pr-preview-kind
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: preview.localtest.me
      allowedRoutes:
        namespaces:
          from: Same
