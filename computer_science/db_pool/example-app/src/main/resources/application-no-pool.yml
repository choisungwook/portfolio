spring:
  datasource:
    url: jdbc:mysql://mysql:3306/testdb?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: rootpassword
    hikari:
      maximum-pool-size: 1
      minimum-idle: 0
      connection-timeout: 1000
      pool-name: HikariPool-NoPool
