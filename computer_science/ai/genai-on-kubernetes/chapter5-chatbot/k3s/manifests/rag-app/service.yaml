apiVersion: v1
kind: Service
metadata:
  name: rag-app-service
  labels:
    app.kubernetes.io/name: rag-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-app
  ports:
    - name: http
      port: 80
      targetPort: 80
