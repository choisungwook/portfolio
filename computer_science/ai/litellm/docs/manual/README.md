# manual 트랙: 웹 UI로 gateway를 구성한다

[set-model/](../../install/set-model/)이 모델·설정을 config.yaml에 **선언**하는 방식이라면, [manual/](../../install/manual/)은 빈 gateway를 띄우고 웹 UI(/ui)에서 모델·virtual key·team을 **손수** 등록하는 방식이다. 같은 gateway를 두 방식으로 구성해 보며, 무엇이 config로 굳고 무엇이 런타임에 쌓이는지 익힌다.

## 문서

| 문서 | 다루는 것 |
|---|---|
| [web-ui-setup.md](web-ui-setup.md) | 웹 UI에서 모델 등록·virtual key·team·user 구성 |
| [../../install/manual/docker-compose.md](../../install/manual/docker-compose.md) | manual/ compose·env·volume·port·DB·UI 로그인 |
