# 실제 값을 채운 뒤 .env로 복사한다. .env는 커밋하지 않는다.

# provider API key. config.json이 env.OPENAI_API_KEY 형태로 참조한다.
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...

# 설정 스토어에 저장하는 key를 암호화하는 키. 한 번 정하면 바꾸지 않는다.
BIFROST_ENCRYPTION_KEY=change-me-32-bytes-long-secret!!
